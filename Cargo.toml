[workspace]
members = [".", "crates/mejiro-core", "crates/mejiro-rmk"]
default-members = [".", "crates/mejiro-core", "crates/mejiro-rmk"]
resolver = "2"

[package]
name = "rmk-mejiro"
version = "0.1.0"
authors = ["n18011"]
description = "Mejiro31 input engine ported to Cygnus-M RMK firmware"
homepage = "https://github.com/n18011/RMK-Mejiro"
repository = "https://github.com/n18011/RMK-Mejiro"
readme = "README.md"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
mejiro-core = { path = "crates/mejiro-core" }
mejiro-rmk = { path = "crates/mejiro-rmk" }
rmk = { version = "=0.8.2", default-features = false, features = [
    "storage",
    "vial",
    "vial_lock",
    "split",
    "mejiro",
] }

[target.'cfg(target_arch = "arm")'.dependencies]
rmk = { version = "=0.8.2", features = [
    "defmt",
    "nrf52840_ble",
    "adafruit_bl",
] }
nrf-sdc = { git = "https://github.com/alexmoon/nrf-sdc", rev = "11d5c3c", features = [
    "defmt",
    "peripheral",
    "central",
    "nrf52840",
] }
nrf-mpsl = { git = "https://github.com/alexmoon/nrf-sdc", rev = "11d5c3c", features = [
    "defmt",
    "critical-section-impl",
    "nrf52840",
] }
bt-hci = { version = "0.6", default-features = false, features = ["defmt"] }

cortex-m = "0.7.7"
cortex-m-rt = "0.7.5"
embassy-time = { version = "0.5", features = ["tick-hz-32_768", "defmt", "defmt-timestamp-uptime"] }
embassy-nrf = { version = "0.8", features = [
    "defmt",
    "nrf52840",
    "time-driver-rtc1",
    "gpiote",
    "unstable-pac",
    "nfc-pins-as-gpio",
    "time",
] }
embassy-executor = { version = "0.9", features = [
    "defmt",
    "arch-cortex-m",
    "executor-thread",
] }
defmt = "1.0"
defmt-rtt = "1.0"
panic-probe = { version = "1.0", features = ["print-defmt"] }
static_cell = "2"

rand = { version = "0.8.4", default-features = false }
rand_core = { version = "0.6" }
rand_chacha = { version = "0.3", default-features = false }
embassy-usb-driver = "=0.2.0"

[patch.crates-io]
rmk = { path = "vendor/rmk-0.8.2" }

[build-dependencies]
xz2 = "0.1.7"
json = "0.12"
const-gen = "1.6"

# Split keyboard example
[[bin]]
name = "central"
path = "src/central.rs"

[[bin]]
name = "peripheral"
path = "src/peripheral.rs"

[profile.dev]
codegen-units = 1      # better optimizations
debug = true
opt-level = 1
overflow-checks = true
lto = false
panic = 'unwind'

[profile.release]
codegen-units = 1       # better optimizations
debug = true            # no overhead for bare-metal
opt-level = "z"         # optimize for binary size
overflow-checks = false
lto = "fat"
